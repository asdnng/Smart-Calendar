{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd MG/src/main/frontend && npm install && npm run build && cd ../../.. && ./gradlew bootJar -x test && chmod +x start.sh"
  },
  "deploy": {
    "startCommand": "cd MG && bash start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

